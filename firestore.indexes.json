{
  "comment": [
    "Composite indexes Firestore can't infer. Deploy with:",
    "  firebase deploy --only firestore:indexes",
    "Everything else in this codebase sorts in memory precisely to avoid needing",
    "indexes; the entries here exist where reading a whole collection would cost",
    "a document read per stored row on every page load."
  ],
  "indexes": [
    {
      "comment": "Inbox: newest-N received mail. Without this, GET /api/emails/messages reads EVERY message a provider has ever received, on every load and every live refresh.",
      "collectionGroup": "emailMessages",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "tenantId", "order": "ASCENDING" },
        { "fieldPath": "at", "order": "DESCENDING" }
      ]
    },
    {
      "comment": "Dashboard: the runs with a session still to come. Without this, GET /api/dashboard reads every block the tenant has ever run, on every load.",
      "collectionGroup": "blocks",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "tenantId", "order": "ASCENDING" },
        { "fieldPath": "endDate", "order": "ASCENDING" }
      ]
    },
    {
      "comment": "Dashboard 'new this week' (and any other recent-bookings window). Without this it reads every booking the tenant has ever taken to count seven days of them.",
      "collectionGroup": "bookings",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "tenantId", "order": "ASCENDING" },
        { "fieldPath": "createdAt", "order": "ASCENDING" }
      ]
    },
    {
      "comment": "Dashboard 'taken this week' — settled card/offline payments by the date the money landed. Pairs with the createdAt index below (a record has one or the other).",
      "collectionGroup": "payments",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "tenantId", "order": "ASCENDING" },
        { "fieldPath": "paidAt", "order": "ASCENDING" }
      ]
    },
    {
      "comment": "Dashboard 'taken this week' — payment records that never got a paidAt (offline records are stamped createdAt only).",
      "collectionGroup": "payments",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "tenantId", "order": "ASCENDING" },
        { "fieldPath": "createdAt", "order": "ASCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}
