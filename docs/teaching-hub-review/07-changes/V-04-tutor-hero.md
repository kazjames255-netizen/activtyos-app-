# Tutor hero at 1440: tighter

**What.** `NextLesson.tsx`: min height 240 to 200, padding p-6/7 to p-5/6, gap above the countdown row pt-5 to pt-3, shrinking the empty band between avatars and countdown.

**Revert.** `git revert`.
