# P-06 Tutor Home = Today (reduced)

**What.** Home already leads with Today (next lesson beside "Needs your attention": to mark, written answers, overdue, quiet). Two dead ends fixed:
- "Needs a nudge" now preselects that student in the homework form (reuses the existing `childIds` hub intent). The chip reads "Set homework".
- "Overdue homework" opens Homework on the "Not handed in" list instead of To mark (`requestHomeworkFilter` / `takeHomeworkFilter` in `hubIntent.ts`, consumed once on mount).
- View-only staff: nudge rows open the student's progress instead (no edit action).

**Why.** T1-02/03/04: nudge and overdue dropped the tutor on the wrong screen. Dropped per the challenge log: duplicate "Set homework" button, parent message template. Tab order and Home quick actions untouched. Stat tiles are P-02 (Builder B).

**Files.** `features/learninghub/home/TutorHome.tsx`, `home/ClassSnapshot.tsx`, `hubIntent.ts`, `homework/TutorHomework.tsx`.

**Before to after (taps).**
- Nudge to form with student ticked: Home, Nudge, Homework tab, Set homework, tick student = 4 taps, to Home, Nudge = 1 tap (form open, student ticked).
- Overdue to overdue list: Home, Overdue, filter "Not handed in" = 2 taps, to 1 tap.
- Marking, setting homework from quick actions, Rejoin: unchanged.

**Revert.** `git revert` this commit; additive helper only.

**Tests.** `npx tsc --noEmit` clean. Home spec selectors kept (`#hub-home-tutor`, "Needs your attention", Homework to mark). Screenshots in `screenshots/after/`.
