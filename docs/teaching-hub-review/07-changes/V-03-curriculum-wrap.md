# Curriculum summary wraps

**What.** `CurriculumCard.tsx`: tutor one-line summary uses `line-clamp-2` instead of `truncate`, so at 390 it wraps to two lines instead of cutting "1 thin ...".

**Revert.** `git revert`.
