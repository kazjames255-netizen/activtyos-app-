# Simplicity-hawk deletion #1: one homework-setting form

**What.** Every "set homework" entry now goes through `HomeworkForm`. Removed:
- `lesson/HomeworkForLesson.tsx` (one-click direct post) from the lesson reader (`NotesPanel`) and the live remote-sync workspace (`RemoteSyncApp`, "Send to class" keeps Flashcards).
- In-person `sendToPortals` (`inperson/api.ts`, `InPersonApp.tsx` start path, the Yes/No segmented step in `SetupStep.tsx`). The step is now one sentence pointing to Set homework.
- The "Set for children" list icon on each Lessons row (`setForChildrenFromList`).

**Callers verified (prefill / deep links kept).** Lesson reader button `lesson-set-for-children` (`lessonHomeworkIntent` with pack prefill, spec oak-journey:374 and assign-lesson:135); quiz card assign (`AssessmentList onAssign`); Home quick action and Students actions (`setHubIntent`); in-call `initialChildIds`; Nudge (P-06). `lessonHomeworkDraft` / `lessonHomeworkIntent` stay in `hubIntent.ts`.

**Why.** Three parallel ways to create homework with different due dates and no group or video options (hawk #1).

**Before to after (taps).** Set this lesson as homework from the reader: 1 tap plus students, to Set for children (form prefilled) then Assign: +1 tap for the tutor who used the inline shortcut, 0 for everyone else. In-person "also send": 1 toggle, to Set homework from Home after the lesson (the trade-off is logged in 11-open-questions.md).

**Revert.** `git revert`; the deleted component is restored from git.

**Tests.** `npx tsc --noEmit` clean. `e2e/learning-hub-inperson.spec.ts` updated: no portal choice, and starting a session creates no homework.
