# P-07 Accessibility (WCAG 2.2 AA, the fixable set)

**What / why.** Contrast, focus, Escape, tablist and live-region fixes from review X1. All tokens live inside `HubStyles` (`#learning-hub`); `app/globals.css` untouched.

**Files.** `features/learninghub/kit.tsx` (tokens, Modal/RowMenu use the layer stack), new `escapeLayer.ts` (`useEscapeLayer`), new `onBrand.ts` (`useOnBrand`, `onBrandFor`), `HubTabs.tsx`, `LearningHubApp.tsx`, `teachKit.tsx` (Sheet, MoreMenu), `YearGroupPicker.tsx`, `curriculum/AreaDrawer.tsx`, `curriculum/CurriculumCard.tsx` (tablist only), `tools/ToolHost.tsx` (Escape only), `shared-assess/ui.tsx` (Escape only).

**Before -> after (ratios on #fff).**
- `--green` inside the hub #15b364 (2.74) -> #0b7a44 (5.41): white-on-green fills and green text pass.
- New `--hub-gold-ink` #7a5300 (6.85), `--on-gold` #171534 (9.9 on gold) replace hard-coded tab "Soon"/badge colours.
- Form-control borders `--line` (1.2:1) -> `--hub-control-line` #85849b (>= 3:1), applied to all hub input/select/textarea (not checkboxes/radios/range); decorative card borders unchanged. `--ink-3` was already fixed to #6b6788 (5.36).
- `--on-brand`: white by default (10.2 / 4.98 on the default blues); `useOnBrand` recomputes it from the tenant's `--brand-2`/`--brand` (dark ink #171534 when white would fall under 4.5:1) and re-runs when ParentBrandTheme rewrites the root style. Applied to the active tab pill and to every `linear-gradient(180deg, var(--brand-2), var(--brand))` button via one HubStyles rule.
- Focus: after a click / Enter / link / `goTo` tab switch, focus moves to the panel's first h1/h2 (or the panel) with `preventScroll`; arrow-key roving leaves focus on the strip. `document.title` = "<tab> - Teaching Hub|My Classroom" (restored on unmount).
- Escape: one module-level stack; only the topmost open layer closes and the key is swallowed (window capture). Registered: Modal, RowMenu, teachKit Sheet + MoreMenu, YearGroupPicker, AreaDrawer, ToolHost, shared-assess Modal. Fixes the ToolHost vs assessment dialog conflict (tool opened over a quiz dialog closed the wrong one).
- CurriculumCard subject tabs: ids, `aria-controls`, roving tabindex, arrow/Home/End, `role="tabpanel"` region.
- Panels get `data-hub-panel`, `aria-busy` while loading; polite `role="status"` line "Showing <child>" for the family child switch. `SkeletonRows` already had `role="status" aria-busy`. HubHero (Builder B's file) untouched.

**Not done.** Bulk replacement of remaining `text-white` on non-gradient fills; contrast on subject palettes (already ink-paired); HubHero radiogroup wiring (B's file).

**Revert.** `git revert` the commit.

**Tests.** `npx tsc --noEmit` clean; eslint: only pre-existing findings; Playwright smoke (tutor/parent/kid, 390 + 1440) and `learning-hub.spec.ts` roving-tabindex test.
