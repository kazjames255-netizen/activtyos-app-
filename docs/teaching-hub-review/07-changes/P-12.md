# P-12 First-run for a new tutor

**What.**
- Zero active students (and nothing failed to load, not view-only): Home shows a 3-step card as its empty state: 1 Add a student (Students tab), 2 Pick a lesson (Lessons), 3 Set homework (opens the homework form). The zero-count attention card, class snapshot, nudges, activity and rhythm chart are hidden. Next lesson and the quick actions stay. At one student the guide is gone and Home is the normal Today view.
- Homework lesson picker gets Year and Subject filters (`hub-hw-note-year`, `hub-hw-note-subject`). Year options are the tenant's free-text `yearGroups`; the filter is hidden when the tenant has none. Year defaults ONLY while exactly one student is ticked and that student's year matches an option; otherwise "All years". Choosing a value overrides the default. Subject list = subjects of the visible topics (hidden if only one). Both go to the existing `/notes` `year=` / `subject=` params (year sent as its number).

**Why.** T5: no first-run guidance; the picker had no Year/Subject filter. Enrol-by-name stays on the roadmap (R-13).

**Files.** `home/TutorHome.tsx`, `homework/hwPickers.tsx`, `homework/HomeworkForm.tsx`.

**Before to after.** New tutor: blank zeros and 5 quick actions, to 3 labelled steps, each 1 tap. Finding a Year 5 maths lesson for one student: search only, to picker already on Year 5. No added taps for tutors with students.

**Revert.** `git revert`. No server or data changes.

**Tests.** `npx tsc --noEmit` clean; `#hub-hw-note-search` and checkbox labels unchanged. Screenshots in `screenshots/after/`.
