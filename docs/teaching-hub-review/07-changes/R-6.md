# R-6 / R-7: age bands (KS1 icon tabs, teen Home)

Band = `bandOrDefault(enrolment.yearGroup)` (family/kidCopy.ts, unknown = KS2). No override UI (needs an enrolment field, logged in 11).

- KS1 (Reception-Y2): family/KidIconTabs.tsx replaces the tab strip with three big tabs: Today (home), Play & learn (quizzes; lit for notes/quizzes/homework/flashcards/diagnostic/messages deep links), Stars (dashboard). Same allow-listed KID_TABS, no new route; a non-allowed `?tab=` still falls back to Home.
- KS2: unchanged.
- Y7+ (ks3 and teen): `useKidCopy().kind` is now KS1/KS2 only, so plain words apply ("Due", "Was due Tue", "Handed in", "Marked"). Home = TeenHome in home/KidHome.tsx: due-this-week rows (homework, cards, next quiz), one Start per row, "Nothing due." when empty, and a "Weakest topics" strip + "Revise weakest" from the child's own /mastery topics (hidden when none). No big card, mascot, streak, confetti or parent card. Support profile `calm` still wins (streak/flame stay off).
- Server (additive, one line of behaviour): GET /students for a family now returns the child's own `yearGroup`; before, parents never received it so every band fell back to KS2.
- Spec: e2e/review/age-bands.spec.ts (Year 1, 4, 9; 390 and 768). Screenshots: screenshots/after/bands/.
