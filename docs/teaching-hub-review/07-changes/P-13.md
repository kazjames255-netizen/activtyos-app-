# P-13 Kind child language

Files: `family/kidCopy.ts` (new string table, age bands, `useKidCopy`), `homework/hwTypes.ts` (`dueState(..., kid)`), `shared-assess/ResultView.tsx`, `shared-assess/ResultBanner.tsx`, `shared-assess/TakeAssessment.tsx`, `shared-assess/StudentAssess.tsx`. The child-screen edits in `homework/StudentHomework.tsx` and `home/StudentHome.tsx` landed in the P-04 commit (same files).

- Kid mode only, and only below Year 10 (band from the enrolment `yearGroup`; unreadable year falls back to a KS2-style band, so kind wording stays on). Year 10+ in kid mode gets the factual wording.
- "Overdue" becomes "Waiting for you" (or "Waiting for you since Tuesday"), gold not red, homework icon not warning. "Handed in late" and "· late" become "Handed in" / nothing. "Awaiting marking" becomes "Handed in".
- Quiz result: no "N points short of the X% pass mark", no "N points to go" chip, no pass-mark chip or ring marker; headline "Nearly there", body "Look back at the answers below, then have another go." Percent text on marked homework is hidden. Ring aria-labels unchanged.
- Kept: "Not quite" on a single answer (six specs assert it), tutor and parent wording, ParentGate.
- Every child-facing string added lives in `KID_COPY`.
