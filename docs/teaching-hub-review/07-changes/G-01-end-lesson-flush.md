# G-01 End lesson flushed on unmount
TutorLiveBanner is mounted separately on Home and Lessons; switching tab inside the 6 s Undo used to cancel End. Unmount now sends /end for the pending session (Undo still cancels inside the window when the tab stays).
