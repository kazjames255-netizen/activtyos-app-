# P-09 Curriculum map out of the way
- `curriculum/CurriculumCard.tsx`: grid closed by default (`pref.open === true`), remembered per device in localStorage `hub.curriculum.v2` (try/catch); the map loads while closed so the summary line (first element on Lessons) always has data. Child header shows a row of 5 stars (no "N of M"); the child subject headline also uses stars.
- `ProgressPanel.tsx`: removed the duplicate tutor `CurriculumRings`; the child overlay stays. `CurriculumRings.tsx` child line is now stars.
- Tenant off switch: not built (see 11-open-questions #10).
