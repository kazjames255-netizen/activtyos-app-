# P-04 One truth for "do I have homework?"

Files: `homework/StudentHomework.tsx`, `homework/homeworkFeed.ts` (new), `homework/RetryFace.tsx` (new), `home/StudentHome.tsx`, `home/useHomeData.ts`.

- A failed homework load is never the empty state. Homework tab and Home card show a friendly face and a 56px "Try again" button (child: "Oops! Let's try again."). "No homework right now" only follows a successful empty response.
- Home and the Homework tab read the homework list through one shared fetch (`getShared`, 8 s reuse, failures never cached, realtime and Retry force a re-read).
- Home lead line (parent/tutor wording): "N overdue" and "N due soon" are separate; kid mode says "waiting for you" (see P-13). "All caught up" is not shown when the homework load failed.
- Not changed: tutor-preview guard, server, API.
