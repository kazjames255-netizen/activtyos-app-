# P-10 One vocabulary (rename + alias map; no tab merge)

**What / why.** Per the challenge log: no merge that adds a tap. Tabs, keys and TabKey are unchanged (kid tab list stays the explicit allow-list). Names now agree, and a small alias map guards `?tab=` links.

**Files.** `features/learninghub/tabAlias.ts` (new: `TAB_ALIAS`, `resolveTab`), `LearningHubApp.tsx` (both `?tab=` reads - initial state and the `linkSearch` effect - go through `resolveTab` BEFORE the module / kid allow-list checks; "Messages" special case removed), `QuestionsPanel.tsx`, `DiagnosticPanel.tsx`, `home/TutorHome.tsx`, `progress/ProgressView.tsx`, `quiz/AssessmentList.tsx`, `quiz/TutorAssess.tsx`, `inperson/CaptureGrid.tsx`, `inperson/SetupStep.tsx`, `live/workspace/QuizTab.tsx`; specs `e2e/learning-hub.spec.ts`, `learning-hub-quizzes-ui.spec.ts`, `learning-hub-home.spec.ts`.

**Before -> after.**
- Tab "Placement test" -> "Starting quizzes" (tutor + parent; child already "Starting quiz"); list/labels/"to mark" copy follow.
- Tab "Student message centre" (tutor) -> "Messages" (same as family).
- Home quick action "Assign homework" -> "Set homework". The form's own submit button "Assign homework" and the "Set homework" segment were left (five specs click them by role).
- Aliases: placement, placement-test, starting, starting-quiz(zes) -> diagnostic; messages/message/message-centre -> questions; lessons -> notes; progress -> dashboard. Existing keys (`diagnostic`, `tools`, `questions&open=doubt:...`, `family/link.ts` and `hubNotify` links) resolve as before; an alias to a tab a child may not see still falls to Home.

**Not done (deliberately).** Tabs 11 -> 9 merge, Segmented sub-switches (adds a tap; waits for R-2 one Mark queue). "Placement test" still appears in D-owned `shared-assess/ResultView.tsx` / `TakeAssessment.tsx` (tutor sentences) and in the builder's "A quiz or placement test" creator tab (an inperson spec clicks it by name).

**Revert.** `git revert` the commit.

**Tests.** tsc clean; tsx check of `resolveTab`/`onBrandFor`; Playwright: learning-hub.spec.ts, learning-hub-quizzes-ui.spec.ts, learning-hub-home.spec.ts, and a deep-link smoke (`?tab=diagnostic`, `?tab=tools`, `?tab=questions`, `?tab=starting-quizzes`, kid `?tab=diagnostic`).
