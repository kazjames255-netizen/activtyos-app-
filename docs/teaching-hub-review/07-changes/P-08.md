# P-08 Tools tab honest by default
- `tools/ToolsPanel.tsx`: only `status: live` tools are listed; unreleased tools are hidden outright (no fold, no toggle, no "In build/Coming soon" badge, demand-logging note removed). Counts per subject chip and "N ready to use" now reflect live tools only.
- `kit.tsx`: `PANEL_ICON.tools = "layers"`.
- Revert: restore `REGISTRY` in place of `READY` in ToolsPanel.
