# P-05 Friendly errors (with P-13 error copy)

**What / why.** Hub error banners showed developer text ("The server didn't respond within 15s (http://localhost:4000). Is the API running?"). Now a network failure is one plain sentence plus a "Try again" button. Per the challenge log: no tutor "Details" toggle; child mode shows "Oops! Let's try again." with a 56px button.

**Files.** `features/learninghub/kit.tsx` (`friendlyError`, `isOfflineError`, `ErrorBanner` gains `onRetry`/`hub`/`kid`), `features/learninghub/LearningHubApp.tsx` (passes `onRetry={refresh}`, hub name, kid; gate reuses `isOfflineError`). `lib/api.ts` untouched.

**Before -> after.** Raw message with URL -> "We can't reach the Teaching Hub / My Classroom right now. Check your connection and try again." + Try again (44px; dismiss button now 44px). Non-network errors pass through unchanged and get no retry button.

**Revert.** `git revert` the commit.

**Tests.** `npx tsc --noEmit` (only unrelated in-progress CurriculumCard errors from another builder); lint has no new findings.
