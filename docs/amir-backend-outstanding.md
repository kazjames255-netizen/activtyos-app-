# ActivityOS — Backend still outstanding (handover for Amir)

_Compiled from the running handoff log. Front-end for all of the below is built and live; these are the server-side pieces owed. Detailed specs live in the referenced `docs/*.md`._

> **Verified against the code on 12 Sept 2026.** Every item below was re-checked
> line by line, because the list had drifted: **eleven items were already built**
> and have been removed (they're in the resolved list at the foot), and nine more
> were overstating what's left and have been reworded down to the real gap.
> Numbering is unchanged so older references still resolve — gaps in the sequence
> are deliberate. Please re-check before starting anything: a stale "owed" item
> costs you a day proving it already works.

**Read first — endpoints already built (do NOT rebuild):** for features that had no backend, thin Express+Firestore routes were written so they'd work (read/aggregate only, nothing money- or security-critical): `subscription.ts`, `platform.ts`, `analytics.ts` (pageview ingest), `inventory.ts`, plus route additions noted per item. Genuinely yours = real Stripe, server-side enforcement, schedulers/notifications, and the collections listed below.

---

## 0. Production blockers (do these first)

1. **Move Firestore project to Blaze** — free-tier daily read cap has been hit in the day (`8 RESOURCE_EXHAUSTED`), blanking registers/bookings. This is the real fix and also unblocks Storage (#2). Add backoff/circuit-break so the scheduler sweeps stop hammering reads while quota is blown.
2. **Firestore Storage bucket** — images/PDFs/signatures/video are still stored as Firestore docs via `/api/images/:id` (≤900 KB, image only; **video upload is impossible today**). Enable Storage, add a real upload endpoint (image + video), swap the backing store in `uploads.ts`. Everything tagged "ties to Storage #1" depends on this.
4. **Deployment** — guide: repo-root `DEPLOY.md`. Code is deploy-ready (all URLs env-driven, `nixpacks.toml`, service-account via secret). Amir does the accounts/console/DNS: Railway (always-on — background sweeps), Vercel, secrets, **Firebase Authorized domain** (this is what broke Susan's signup link on mobile), Stripe webhook, Namecheap DNS.
5b. **Per-franchise settings — the remaining readers** — *mostly fixed 12 Sept.* `server/src/lib/tenantLibrary.ts` (`loadLibrary`/`loadSettings`/`librarySnap`, franchise copy with head-office fallback, plus `franchiseForChild`) is now used by everything that enforces or decides something: medication gates + notifications, trips (consent gate, who can plan, notify), incident/accident parent notifications, staff roster policy + ratios, cancellation policies (parent cancel + partial cancel), meals ordering/cut-off, the customer page's venue/add-ons/staff slice, and Setup → Notifications (a franchise's alerts also email the franchise, not head office). **Still reading head office's copy:** `lib/emails.ts`, `lib/autoEmails.ts`, `lib/sender.ts` (branding + automatic emails), `lib/sweeps.ts` per-tenant prefs (session reminders, late-collection threshold), `routes/referral.ts`, `routes/memberships.ts`, `routes/reviews.ts`, `routes/posts.ts`, `routes/invites.ts` (invite message). Also still to decide: which settings a franchise may override at all — safety gates probably shouldn't be loosenable below head office's.

5. **Franchise data isolation — staff scoping + backfill** — spec: `docs/franchise-isolation-handoff.md`. **Most of this is now done** (verified 12 Sept): the franchise-ownership key on listings, HO assignment, and the read filter are all live, and listings, registers, safeguarding/incidents/medication, meals, moments, customers, messages, invoices, expenses, income and purchasing all scope through `lib/franchiseScope.ts`. **Still owed:** staff/team scoping (there is no `staff_assignments` collection — invites carry only `franchiseId`), and a backfill to tag data created before scoping existed.

## 1. Money & payments

7. **Customer memberships — recurring charge** — spec: `docs/memberships-handoff.md`. Phase 1 delivers benefits on JOIN with no money collected. Owed: Stripe subscription per member on the provider's connected account; `invoice.paid` webhook → call existing `deliverMembershipBenefit()`; `payment_failed` → dunning → suspend perks; cancel → cancel sub. **Idempotency**: dedupe credit path on `lastDeliveredAt` (double-credits on duplicate webhooks). Do NOT ship to real money without gating delivery behind a successful charge.
8. **Reconciliation — Tax-Free Childcare auto-reconcile** — wire the HMRC EPP integration (parents pay by Ofsted no.) to match TFC money to bookings and flip them Paid. TFC tab is deliberately blank pending this. (`docs/listings-backend-handoff.md` §S.)
9. **Reconciliation — `cardFailed` trigger** — field + banner exist; set `cardFailed:true` from the Stripe `payment_intent.payment_failed` webhook, clear on success.
10. **Reconciliation phase 2** — split payments (a booking paid across card + TFC/voucher — needs a payment-portions model `[{method,amount,status,ref}]` on the booking, reflected parent + provider), per-child sibling references, parent enters their reference once at checkout. Ripples through parent checkout, booking display, reconciliation — build carefully.
12. **Add-ons in partial refunds** — per-day add-ons need structured per-day prices on the booking (currently flattened strings); partial-refund calc should = that day's pass share + that day's add-ons at policy %. Refund engine + data model.
15. **Rota labour cost → Money/Dashboard/Finance/Payroll** — *correction 12 Sept:* the Schedule screen never used `/api/shifts` — it was browser-only (`aos.rota.v5`), so the "persisted" claim was wrong and the DBS/compliance roster gate was unreachable. It now saves to **`/api/rota`** (`routes/rota.ts`: roster meta in `rotas/{key}`, one doc per shift in `rotaShifts`; every new assignment is checked with `staffRosterBlock`, refused 409 naming who and why; staff GET it without pay rates). `/api/shifts` is still unused by any screen — fold it into `rotaShifts` or delete it. **Still owed:** `users.pay_rate`, `GET /api/labour-cost?from=&to=`, and the roll-up of shift hours × rate × on-cost into Money-out, Dashboard profit, Finance and Payroll (the rota's "Total wages" is display-only).
16. **Setup money toggles that persist but nothing reads** — "Issue refunds automatically" / "credit note when no cash due" / "Ask about dietary needs".

## 2. Notifications & schedulers (the big theme — most FE flows send nothing yet)

22. **Timetable publish → notify parents** — `publishSchema` accepts `notifyEmail`/`notifyPush` but does nothing; on publish-to-parents send an email digest + in-app notifications to the audience.
23. **Meal-order notifications — the parent half** — *provider half done 12 Sept:* a new order, and a change/removal request under approval, now bell (+ email, Setup key `meal-order`) the provider (`routes/mealsShop.ts` `mealAlert`). **Still owed:** tell the PARENT when the provider approves or declines their change/removal request.
24. **Caterer digest cron** (#32a) — email `listing.mealConfig.catererEmail` a digest of that listing's meal orders daily/weekly at `catererAt`. No sweep reads those three fields, so the kitchen is told nothing; the UI now warns the provider of that. (The checkout meal cut-off mentioned here before is **done** — `my.ts` rejects meals for closed dates.)
25. **Schedule — the sends** — the availability-request flow itself is **built** (create, per-staff bell, staff submit screen, `pending→submitted`, operator assigns days). **Still owed:** the EMAIL half of that request (bell only today), and all four `settings.scheduling` sends — notify-on-publish, `autoRemindUnconfirmed`, `shiftReminder` (24h/2h), `autoRequestAvailability`. None of those keys is read anywhere server-side.
26. **Learning reminders** — *half done 12 Sept:* "Assign & notify" and "Remind" now really bell + email the covered staff (`POST /api/learning/notify` resolves real staff accounts by all / job role / name and returns the count; the toast says it). **Still owed:** the SCHEDULED part — due/overdue/renewal-due/unread-policy chasing and the weekly manager digest — which needs the LMS assignments/completions on the server (#37).
27. **Holiday approvals** — *done 12 Sept:* `routes/leave.ts` + `features/holiday/data.ts` — requests, decisions and cancellations are server-side (staff can only ask for themselves; managers decide, stamped); the manager is belled on a request (Setup key `leave-request`), the person belled + emailed on the decision; colleagues' sickness shows to staff only as "away". **Still owed:** the "needs covering" alert against the rota, and #40 below.
28. **Staff announcements delivery** — *done 12 Sept:* `routes/staffAnnouncements.ts` — posts stored per tenant/franchise (head office can target one franchise), each staff member belled (emailed when important), per-person read receipts, managers see read counts; the three made-up seed notices are gone. **Still owed:** site/listing-level targeting (needs staff assignments, #32) and letting LEADS post (needs a lead level, §8 #57).
29. **Message merge-fields — the two remaining paths** — resolution IS built for `POST /api/messages/from-booking` (all seven tokens) and for bulk Email sends (per recipient, from that family's best booking). **Still owed:** the plain 1:1 `POST /api/messages` handler never calls `mergeText`, so tokens go out raw; and the message-family broadcast fills only 4 of 7 (no `SessionDate`/`VenueName`/`BookingRef`).

## 3. Staff, RBAC & per-user identity (many items share these roots)

30. **Roles & permissions enforcement** — spec: `docs/roles-permissions-handoff.md`. `role_id` on invites + users (assign on activation, existing = Owner); add `role`+resolved `caps` to `/api/me` (unblocks FE sidebar/action gating); **server-side capability enforcement** per area; Phase 2b sensitive-data hard-enforce (medical/SEND/payroll/finances) + audit log.
31. **Team & invites store** — *done 12 Sept:* `staffRole`, `jobTitle`, `assignment` and `name` are stored on the invite, returned by `GET /api/invites`, and copied onto the user on accept (with the invite's `franchiseId` — a franchise's staff were getting tenant-wide scope); `PATCH /api/invites/:token/status` exists and really disables the account (`users.disabled` + `revokeRefreshTokens`, refused in `middleware/role.ts`). The accept is bound to the invited address. **Still owed:** `users.role_id` (item 30), and requiring a VERIFIED email for the address binding once verification mail is on (today it matches `user.email`, which Firebase lets an unverified signup claim).
32. **Deployment / assignment scoping** — accepted invite → person appears in Deployment under assigned location/listings; assignment scopes registers/attendance (assigned staff see only their listings); availability → schedule. Persist `staff_assignments(user_id, venue_id, tenant_id)`.
33. **Staff-onboarding invite pre-fill** — feed the real invite record's captured name/email/role into the onboarding form (demo uses a hardcoded const).
34. **Per-user identity** — *partly fixed 12 Sept:* My schedule, the staff dashboard and My leave now use the signed-in account's name (the hardcoded "Marcus Bell" is kept only for the guided-tour demo). **Still hardcoded:** My certificates, My payslips, My learning.
35. **Register — child edits + notes** — *done 12 Sept:* `childId` is on the attendee projection; `PUT /api/children/:id` (managers, child must be booked with the tenant/franchise) patches care fields with `careHistory`; notes, nappy changes and chase-nudges live on the register doc (`POST /api/registers/:blockId/:date/note|nappy|nudge`, staff archive / managers delete). **Still owed:** a LEAD flag within staff (so leads can edit), and delivering `shareParent` notes to the parent app.
36. **Listing wizard staff picker** — feed from the tenant's real onboarded/invited team (name+role+bio) instead of the separate `local.staff` localStorage list, so onboarding a person makes them available to tick on every listing. (Model is by-reference via `d.staffIds` already.)

## 4. Big feature collections (front-end complete, need real persistence)

37. **Learning Centre (LMS)** — real collections: courses/quizzes (+ items, questions, pass mark), assignments (scope→audience, due, required), per-staff completion + attempts/scores, certificate store; media uploads (video/image lesson content — ties to Storage); build the staff-side LMS collections; register `learning` for franchise too. Policies read-and-confirm: `policies` + `policyAcks` collections, PDF in object storage, notify-until-confirmed.
38. **Staff credentials/certificates** — real file storage for cert uploads (Storage), persist records + verify state, per-user identity, expiry/renewal reminder sends. Build the public **`/v/<ref>` certificate verification page** the QR points to; persist issued certs + ref lookup.
39. **Payroll** — spec: `docs/payroll-integrations-handoff.md`. RTI (FPS/EPS), per-employee YTD store (P60/P45), proper tax-code/NI (incl. Scottish/Welsh, W1/M1), actual hours from rota, statutory pay + student loans + starters/leavers, real OAuth to QuickBooks/Xero/Sage posting the wages journal, persisted payslips + real PDF + email, payroll-admin RBAC + field-level encryption + audit. **Never store payroll/bank/NI in localStorage in prod.**
40. **Holiday & absence** — spec: `docs/holiday-planner-handoff.md`. *Storage, identity and approval RBAC done 12 Sept (see #27).* **Still owed:** carry-over rollover at year end; gov.uk bank-holidays feed by division; holiday-pay → payroll and SSP from sickness; data-protection handling for sickness records (retention/access log).

## 5. Data-model gaps & smaller items

41. **Parent/customer postcode on the BOOKING** — a postcode is **already** captured at parent signup, editable in account settings and returned by `GET /api/me`. **Still owed:** `booking.postcode` at checkout, and adding it to the `/api/bookings` and `/api/customers` projections — the Task Manager parent picker reads it defensively and currently finds nothing.
43. **Listing auto-expire** — scheduled job to flip stored `status` to ended once the last date passes (feed already gates on dates, so nothing leaks — this is for counts/exports/HQ analytics accuracy). Optionally notify the provider.
44. **Newsletter → HTML email** — spec: `docs/newsletter-email-handoff.md`. `postToHtml`/`newsletterToHtml`, **scheduled sends** (`POST /api/emails/schedule` + sweep) and **open tracking** (pixel + ingest) are all **done**. **Still owed:** real PDF/file attachments (only `{name,size}` metadata is stored today, and the mailer's attachment support is never used by the campaign path), click tracking, video storage (ties to #2) and the live countdown GIF.
45. **Marketing consent unsub token** — *signed 12 Sept* (HMAC via `lib/signing.ts`; unsigned legacy tokens honoured until 12 Oct 2026, then refused; the page no longer reflects the address unescaped). **Still owed:** surface the suppression list in the UI.
46. **Seasons — Dashboard date param** — `GET /api/dashboard` (and server-aggregated analytics) should accept `?from=&to=` to scope to a season. Low priority.
46a. **Subscription history for HQ churn %** — ***DONE 26 Sept 2026*** (acceptance p2-m35). Append-only `subscriptionEvents` collection (`{tenantId, type, state, at, month, seq, plan, band, cadence, mrr, subscriptionId, source}`) written from `lib/billing.ts syncFromStripe` — i.e. from the Stripe webhook, the subscription routes and the sync sweep, whichever notices first — plus the record-only paths in `routes/subscription.ts`. Idempotent twice over: nothing is appended unless the tenant's newest event has a different `state`, and doc ids are deterministic (`{tenantId}__{seq}__{type}`, `create`-only), so a replayed webhook or a sweep can't add a second row. New aggregate `GET /api/platform/churn?months=6` → `{months:[{month,label,base,canceled,pct}]}`, platform-only, computed from the log — a cancellation is counted in the month **notice was given**, so `cancel_at_period_end` is visible immediately, and a reactivation no longer erases it. `tenants/{id}.createdAt` audited (all 121 tenants already carried a valid one — 0 backfilled) and the aggregate falls back to the document's own create time, so a null can never sit in every denominator again; existing tenants' history was seeded once from their current record (37 events across 29 tenants, stamped `source:"backfill"`). **Front end still to do (Kaz):** `features/platform/FalloffCard.tsx` must read the new endpoint for its per-month chart instead of deriving it from `/api/platform/providers` — see the handover note. Original write-up below.

    HQ → Analytics → Fall-off now shows a **monthly churn rate** under each month (cancellations ÷ providers on the books when that month opened), computed client-side in `features/platform/FalloffCard.tsx` from `GET /api/platform/providers`. Three server-side gaps make it drift:
    - **`tenants/{id}.createdAt` is not guaranteed** — `platform.ts:181` projects `?? null`. A null is treated as "predates the window", so it silently lands in every month's denominator and flattens the rate. Backfill it and set it on every tenant at creation.
    - **`canceledAt` is only written when status reaches `canceled`** (`billing.ts:185`). A `cancel_at_period_end` provider sits at `canceling` with no date, so the month they actually gave notice is invisible until the term ends — the loss shows up in the wrong month.
    - **History is reconstructed from the *current* subscription doc, so it rewrites itself.** Reactivation clears `canceledAt` (`subscription.ts:352`, `:489`), which erases that provider's past cancellation from the chart — last quarter's numbers change whenever someone returns. Proper fix: an append-only `subscriptionEvents` collection (`{tenantId, type: "trial_started"|"started"|"canceling"|"canceled"|"reactivated", at, plan, band, mrr}`) written from the Stripe webhooks, plus an aggregate endpoint (e.g. `GET /api/platform/churn?months=6 → [{month, base, canceled, pct}]`) so the card reads real history instead of inferring it. Ties to #3 (real Stripe Billing) — do it as part of that webhook work rather than after.

## 6. AI, voice & niceties

47. **AI co-pilot tool-use** — spec: `docs/ai-assistant-tooluse-handoff.md`. `POST /api/ai/chat` **exists** but is deliberately read-only (its prompt says it cannot book, cancel, refund or message). **Still owed:** the `{reply?, action?}` contract, `POST /api/ai/act {tool,args}` executing against existing authed endpoints with a server-side permission re-check and a mandatory confirm for mail/money/irreversible actions; then SSE streaming and server-side saved threads.
48. **Server-side TTS** (#34) — one consistent British female narration voice for the guided walkthroughs + LMS read-aloud (browser `speechSynthesis` varies per browser). TTS endpoint + cached clips; FE swap is small.
49. **AI compose lesson tone** — add a course/e-learning `kind` to `/api/ai/compose` (current prompt is tuned for parent posts); one-line FE swap.

## 7. API docs & cleanup before launch

50. **OpenAPI** — Money area absent from `server/openapi.yaml` (invoices, expenses, POs); document `/api/income`, `/api/suppliers`, purchasing fields, all subscription routes, new platform routes + `/api/analytics/pageview` + register-role fields.
51. **Dead routes / code** — `locations` registered for 3 portals with no nav entry; staff `compliance` registered but absent from staff nav; listing-wizard drafts localStorage-only (don't follow across devices); HowItWorks walkthrough video slots (record or hide); dead code (`ListingsApp.tsx`, `storefront/BookingPanel.tsx`, unused `applyChangeDayMutation`).


## 8. Added by the 12 Sept overnight run — only you can do these

52. **`URL_SIGNING_SECRET` in production** — private images (children's photos, injury photos) and unsubscribe links are now HMAC-signed (`server/src/lib/signing.ts`). Without the env var the key is derived from the service account; in production with neither it falls back to a random per-process key (links break on every restart). Now documented in `DEPLOY.md` and `server/.env.example` — **still needs setting in Railway** (`openssl rand -hex 32`).
53. ~~Run `server/src/backfillPrivateImages.ts` once~~ — **done 12 Sept**: 7 images marked private (bare link → 403, signed link → 200). The old production code ignores the flag, so it's harmless until the new code deploys. Re-run (idempotent) after deploying if more were uploaded in between.
54. **Rate limiting is per process** — `server/src/lib/rateLimit.ts` (in-memory) now guards the public endpoints. Fine on one Railway instance; swap for a shared store before running more than one.
55. **Stripe Connect webhook (`payment_intent.succeeded`)** — still owed (backlog b7): a booking/invoice is only settled if the browser calls back. Invoices now settle their linked booking (`settleInvoiceBooking`) on the pay-link confirm and on manual "paid" — the webhook should call the same function.
56. **Offline refunds need a home** — approving a refund on a TFC/voucher/cash booking now records a `payments` doc `{type:"refund", offline:true, status:"to-reimburse"}` and tells the family it comes back the way they paid. Reconciliation should list those until someone marks them returned.
57. ~~Leads within staff~~ — **done 12 Sept**: `users.lead` (Team & invites → Make lead), `req.auth.lead`, `isPlainStaff()`. Per-permission roles (item 30) are still the bigger job.
58. **Closed parent accounts after 30 days** — signing back in now reopens a closed account (`POST /api/account/reactivate`, called by the login page; `disabled` accounts — switched off by a provider — stay refused). What's NOT built: the Close screen says "within 30 days", and nothing happens at 30 days. Decide what does (anonymise? delete the login?) and add the sweep; until then reopening works forever.
59. **Refunding an invoice-paid top-up** — a paid invoice no longer overwrites the booking's `paymentIntentId` (that's what refunds go back to); the invoice's own intent is kept in `bookings.invoicePaymentIntentIds[]`. A refund larger than the original card payment needs to split across those — the refund route only knows the one.
60. **LMS** — assignments and completions are server-side now (`/api/learning/assignments`, `/completions`). Still per-device: a manager's edits to course CONTENT, in-progress position within a course, the LMS "Policies" tab (duplicates Documents — consider removing it), reminder prefs.
61. **Encryption at rest for the onboarding record** — `onboardRecords` / `onboardFiles` (routes/onboarding.ts) now hold bank details, NI number, passport/ID scans and DBS certificates server-side, access-restricted (manager or the person). They're not encrypted beyond Firestore's default, and there's no retention period. Decide both before real staff records go in.
62. **Payroll is still a demo** — `features/payroll/PayrollApp.tsx` builds its employee list and pay from the demo cast. It now has real inputs to use (team via `/api/invites`, pay rate from the onboarding record, hours from `/api/timeclock`).
63. **Clock pay-policy settings are per-device** (`aos.timeclock.settings.v1`: grace minutes, rounding, pay basis). Move to tenant settings so every manager's timesheet computes the same pay.
64. **The rota's DBS check switches on at the first certificate** — `staffRosterBlock` skips tenants with no certificates at all. Now that Staff certs feed it (routes/credentials.ts mirror), the first certificate anyone records turns enforcement on for the whole team. Correct for safety, but worth telling providers.

## 9. Added 17 Sept — found during Plan 2 acceptance testing

65. **HQ "Open account" impersonation doesn't switch API identity — real cross-tenant leak.** The UI shows the impersonated user, but every write is still attributed to the signed-in platform admin's own token. Reproduced live: booked Provider B's listing while impersonating Provider A's parent — the booking and the child landed under `kazadmin` in Provider B's Families list, AND the same child record appeared on Provider A's own customer list (a child created while booking with B is now visible, by name and age, to A). This is the single most serious item on this list — it's a live data leak between two unrelated tenants' families, not a display bug.
    **17 Sept code read — three places checked, none of them explain it, so look elsewhere:** (1) `server/src/middleware/role.ts` `attachRole` (the `x-act-as` handler, ~line 122): correctly swaps BOTH `req.user` (uid/email/name → the target's) and `req.auth` (role/tenantId/franchiseId → the target's) when the header is present. (2) `lib/api.ts`'s `actAsHeader()` is attached on every authenticated request via the shared `api()`/fetch wrapper (lines ~111, ~171) — not opt-in per route. (3) `server/src/routes/my.ts` POST `/bookings` resolves the booking family via `tokenEmail(req)` → `req.user?.email` and `familyUid = req.user?.uid` — both would already be the swapped identity if the header made it through. So either HQ's "Open account" button doesn't actually go through the `x-act-as` header path at all (worth checking `components/shell/AccountPicker.tsx` / wherever "Open account" is wired — maybe it does a real Firebase sign-in-as / custom-token swap instead, with a bug in THAT), or there's a fourth read of the raw decoded token somewhere in the booking-creation path that bypasses `req.user`. Start there rather than re-checking the three spots above.
66. **Franchise (and operator) Setup → Features toggles reported as inert — NEEDS RE-VERIFICATION, likely a test-timing false positive.** Original finding: every On/Off control in Setup → Features (Meals, Moments, Ratios & groups, Trips & visits) ignored both a real click and a direct DOM `.click()` — no state change, no request to `:4000` — reproduced on a franchise account AND a plain freelancer account. **17 Sept code read (`lib/settings.ts` `useSettings().save`):** the toggle's `onClick` → `onChange` → `set()` chain is intact and does an OPTIMISTIC `setSettings()` immediately, then debounces the real `PUT /api/library` by **500ms** (`setTimeout(() => flush(), 500)`) so rapid toggles coalesce into one save; a flush failure reverts the UI and now shows an inline error (fixed in `b0881d5`, 15 Sept). A test that clicks and checks for a network request in under 500ms would see exactly "no request, no state change" without anything being broken. Re-run the original repro with a ≥1s wait after the click before concluding it's dead — if it's still silent after that, then it's a real bug and this note is wrong.
67. **No print stylesheet, and no PDF generation anywhere in the API.** The served stylesheet has exactly one `@media print` rule, `.print\:hidden { display: none }`, used on a single floating widget — the sidebar/top nav/page chrome have no print-hiding at all, so printing any operator page (payslip, invoice, incident record, certificate) carries the full app chrome into the printout with arbitrary page breaks. Confirmed there's no PDF backend either: the OpenAPI spec (133 paths) has zero `application/pdf` occurrences and no payslip/document-generation endpoint — a booking's detail only offers "Resend invoice" (emails it), not a printable/downloadable view. Needs a decision: real `@media print` rules (hide chrome, `break-inside`/`break-after` on rows/cards) as the cheap fix, or server-rendered PDFs as the real one.
68. **A parent's basket is silently destroyed when they browse to a second listing** — no crash, but also no warning before and no message after. Add a pass to listing A's basket, then navigate to listing B on the same tenant: B's basket is empty, and navigating back to A shows A's is empty too (the item wasn't parked, it was thrown away). Minimum fix: a confirm before switching listings ("Going to another club will clear your basket — continue?") or a line on the new listing saying what was dropped. Multi-listing baskets being unsupported is expected (per the code notes) — the silence around it isn't.
69. **Leftover demo-cast data lives in real accounts' `localStorage`, and the app offers to import it into the real tenant.** On a genuine head-office account (not demo mode), Team & invites → Onboarding renders: "This browser has 1 onboarding record(s) saved before records moved to the server (Marcus Bell). Import them into this account, or discard them if they aren't yours." with live Import/Discard buttons. The same fixture name is embedded across six `aos.*` localStorage keys (onboarding records + backup, staff announcements, staff availability, holiday profiles, holiday absences) on at least one real browser profile. Nobody has clicked Import yet, but the control being live at all risks contaminating a real tenant's onboarding/HR records with fixture data. Needs the import-offer suppressed (or the keys cleared) for any non-demo account — related to item 34 above (Marcus Bell as intentional demo-tour hardcoding), but this is fixture data leaking into a REAL account's UI, not the tour.

## 10. Added 17 Sept — found continuing the e2e triage

70. **A staff account has no way to see an unfilled/open rota shift, anywhere.** `MyScheduleApp.tsx` (the `/staff/schedule` a staff role lands on) only ever renders shifts with a real `staffId` set (`all.filter(sh => sh.staffId)`), even though the server (`routes/rota.ts` GET `/api/rota`) deliberately includes open shifts for every staff account ("open shifts carry no one's details" — by design, so they'd presumably want a "pick up this open shift" surface eventually). Right now an unassigned shift is invisible to the very people who could fill it — worth checking whether this is a known gap or an intended future feature that never got wired to the UI. Also noted while testing this: the "Assign staff" picker (ScheduleApp.tsx) can only offer people already on the tenant's rota roster, which is a separate thing from having a staff **login** — an invited `role: staff` account isn't automatically a rota-schedulable team member. `e2e/settings-scheduling.spec.ts`'s rota test used to assert staff-side visibility of an assigned shift; it's been scoped down to only the franchise-side add-role/add-shift flow because there's no way to provision a real rota-roster member in the e2e fixtures right now, and no UI surface to confirm the staff-visible half against even if there were.
71. **Unresolved, flagged rather than guessed at: a freshly-logged accident record intermittently doesn't reach the parent's `/custdash/accidents` view within 30s, even though the operator side confirms the save (`GET /api/incidents` server-side matching read `resolveChild()` in `server/src/routes/my.ts` looks correct — booking's `childId` gets resolved by name against the parent's existing child record). Read through the realtime invalidation path (`lib/realtime.ts`), the parent-side query (`incidents.ts` GET `/`, which `.slice(0, 10)`s the parent's child ids before an `in` query — a real latent cap worth knowing about on an account with >10 children, though unlikely to be the cause here with only 2-4 children on the shared e2e parent fixture) and `ParentAccidentsApp.tsx` without finding a definitive root cause. Bumped the e2e assertion's timeout as a stopgap (same class of slow-dev-server issue confirmed elsewhere this session), but this one hasn't actually been proven to be that — worth a slower, more deliberate repro (watch the SSE stream / network tab live) if it keeps showing up.
72. **A worded (non-designer) campaign email can never carry a countdown clock — the toggle doesn't exist.** `features/email/EmailApp.tsx` declares `cdOn`/`setCdOn`, `cdDate`/`setCdDate`, `cdTime`/`setCdTime`, `cdHeading`/`setCdHeading` and even computes `wordedHasCountdown = cdOn && !!cdDate`, but none of the setters have a single call site anywhere in the file — the worded-template branch's JSX (`mode === "template"`) has no button or panel to turn it on. The leftover warning copy even says "Open the … ⏱ Countdown panel" for a panel that no longer exists. A countdown is only reachable now via the separate Designer/block-builder flow. Either restore a worded-mode countdown toggle, or (if the designer is now the only intended way to add one) delete the dead `cd*` state and fix the warning text so it stops pointing at a nonexistent panel. `e2e/email.spec.ts`'s "campaign wizard sends a worded email (with countdown)" test is left failing on purpose to reflect this rather than being rewritten to exercise a different flow.
73. **Found and fixed (root cause confirmed, not just papered over): the Templates/Campaign designer's full-screen modal (`campaignTemplates.tsx`) always lost clicks to the persistent header's notification bell.** Not a race or a tie — `EmailApp.tsx` wrapped both `<CampaignDesigner>` mounts in a `<div className="relative z-[130/145]">` that did nothing for layout (the designer's own root is already `position:fixed`) but created a *stacking context* that trapped the designer's z-index inside it. No matter how high the designer's own z-index went, the browser only ever compared the wrapper's z-130/145 against the header's `z-[300]` (`components/shell/Header.tsx`) — confirmed live via `getComputedStyle` + `document.elementFromPoint()` in an actual signed-in session: even after raising the designer to `z-[200]`, clicks on "Save template" still hit the bell underneath. Fixed by deleting both wrapper divs entirely (they served no purpose) and raising the designer's own modal to `z-[350]` (above the header's 300, below the app's toast layer at 900+). Re-verified live: `elementFromPoint` now returns the actual button, and the e2e suite confirms — `e2e/email.spec.ts` and `e2e/email-tools.spec.ts` both pass cleanly now (kept their `force: true` clicks as harmless defence-in-depth, not because they're still needed).

---

_Already resolved (for reference, don't redo): Stripe test key e2e, SMTP (Gmail), real StaffDashApp, HQ analytics/engagement/at-risk endpoints, marketing-consent filtering + unsubscribe, meal-order Stripe charge, signup attribution surfacing, `seasonId` listing persistence._

### Verified built on 12 Sept 2026 — removed from the list above

These were still written up as owed. They are done; the numbers are retired.

| # | Item | Where it lives now |
|---|---|---|
| 3 | Real Stripe Billing | Elements capture; `trial_period_days`; `payment_failed` → `past_due` webhook; **server-side** 402 wall (`middleware/subscription.ts`); staff metering + hard cap. `billingConfigured` is no longer hard-false |
| 6 | Customer wallet backend | `lib/wallet.ts` ledger + `GET /api/wallet/summary → {outstanding}`; credit on cancel |
| 11 | Per-day partial cancellation | `my.ts` `partialCancel()` — `cancelledDays`, per-day capacity release, disabled-resolution validation, wallet credit, notify. `changedate` routes to `/amend` with the FE date picker |
| 13 | Single-use discount released on cancel | `lib/discountRedemptions.ts` `releaseDiscountCodes()`, called from **both** cancel paths |
| 14 | Payment method on bookings | Exists as `method` — captured at checkout, carried by the projection, and the Email→Audiences filter already reads it. The doc had the field name wrong |
| 17 | Accidents/Incidents → parent | `incidents.ts` notifies on log, on edit (gated), and staff on acknowledge; plus the `acknowledgementChase` sweep |
| 18 | Medication → parent | `childId` resolution, `given` boolean, per-dose parent notify, parent med + MAR views, due-time sweep |
| 19 | Booked-child picker link | `GET /api/children/lookup`, wired into both operator forms |
| 20 | Trips & visits — consent + notify | Per-child consent map, consent-request send, `requireConsent` enforced (409 on complete), chase sweep, parent consent view |
| 21 | Calendar reminders | `calendarReminders` sweep — per-event override vs tenant default, idempotent |
| 42 | Provider postcode → browse-by-distance | Server-side geocoding (`routes/geo.ts`), venue lat/lng on the public feed, parent-postcode distance search |

### Fixed on 12 Sept 2026 (overnight run) — see `lib/testing/backlog.ts` for the detail

Staff token reading money/DBS/concerns (b1/b5), public child photos (b2 — signed links; backfill = item 53), no way to switch an account off (b3), invites redeemable by anyone (b4), franchise staff unscoped, bell ignoring scoping (b17), siblings counted as one child on registers/ratios/meals (b18), declined trip consent overwritable by staff (b19), withdrawn medication consent re-grantable (b20), rename not reaching bookings (b21), child vanishing from the register when cancelled on site (b22), listing edit deleting a booked day's register (b23), register notes device-only (b25), partial-payment refunds of £0 (b10), wallet credit lost on cancel (b13), membership credit minted on re-join (b14), invoice not settling its booking (b11), refunds fire-and-forget / double-approvable (b12), TFC refunds promised "to your card" (b9), unsigned unsubscribe (b31), parent text unescaped in emails (b30), parent-uploaded HTML running in the operator's session (EHCP viewer), deletion requests read by nobody + incomplete export (b37), no rate limiting (b39), no not-found/error pages (b34). Then, from the reviewers of that run: dose-email mute now server-side with missed doses never muted (r1), clock stamps on the server rota (r2), parents clearing child fields (r3), finished whole-course bookings blocking child removal (r4), closed accounts reopening on sign-in (r5), invoice settlement keeping the card payment + not downgrading "Paid" (r6), cancel clean-up only on the cancel (r7), membership tier-hop credit (r8), chasing declined referees + HEIC uploads (r9), learning location targeting (r10), calendar events franchise-scoped. Second pass the same day: leads (r11), clock records server-side (r12), Staff certs feeding the rota's DBS check (r13), demo cast removed from real providers' screens (r14), LMS completions/assignments (r15), onboarding/SCR server-side (b24), 7 old photos made private (item 53), and most parent/staff screens translated (b38).
